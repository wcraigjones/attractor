# 03-claude-sonnet-4-5 / 03-go-reverse-string

E2E smoke test: `claude-sonnet-4-5` generates a Go go-reverse-string program, a tool node extracts and runs it, and the test validates the expected output. Skips if `ANTHROPIC_API_KEY` is not set.
