# 08-gemini-3-flash-preview / 07-python-temp-converter

E2E smoke test: `gemini-3-flash-preview` generates a Python python-temp-converter program, a tool node extracts and runs it, and the test validates the expected output. Skips if `GEMINI_API_KEY` is not set.
