# 08-gemini-3-flash-preview / 05-c-sum-integers

E2E smoke test: `gemini-3-flash-preview` generates a C c-sum-integers program, a tool node extracts and runs it, and the test validates the expected output. Skips if `GEMINI_API_KEY` is not set.
