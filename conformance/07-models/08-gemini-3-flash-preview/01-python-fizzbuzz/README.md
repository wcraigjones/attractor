# 08-gemini-3-flash-preview / 01-python-fizzbuzz

E2E smoke test: `gemini-3-flash-preview` generates a Python python-fizzbuzz program, a tool node extracts and runs it, and the test validates the expected output. Skips if `GEMINI_API_KEY` is not set.
