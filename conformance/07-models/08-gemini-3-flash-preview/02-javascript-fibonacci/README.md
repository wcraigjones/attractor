# 08-gemini-3-flash-preview / 02-javascript-fibonacci

E2E smoke test: `gemini-3-flash-preview` generates a Javascript javascript-fibonacci program, a tool node extracts and runs it, and the test validates the expected output. Skips if `GEMINI_API_KEY` is not set.
