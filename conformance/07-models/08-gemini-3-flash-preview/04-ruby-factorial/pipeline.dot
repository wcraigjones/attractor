digraph smoke_gemini_3_flash_04_ruby_factorial {
    graph [goal="Generate and run a Ruby factorial program"]

    start [shape=Mdiamond]
    code  [shape=box, llm_model="gemini-3-flash-preview", prompt="Write a Ruby program that computes the factorial of 10 and prints the result. Output ONLY the code inside a single fenced code block. No explanations."]
    run   [shape=parallelogram, tool_command="bash \"$EXTRACT_RUN\" ruby code"]
    done  [shape=Msquare]

    start -> code
    code  -> run
    run   -> done
}
