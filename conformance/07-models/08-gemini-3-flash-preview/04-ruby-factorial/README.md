# 08-gemini-3-flash-preview / 04-ruby-factorial

E2E smoke test: `gemini-3-flash-preview` generates a Ruby ruby-factorial program, a tool node extracts and runs it, and the test validates the expected output. Skips if `GEMINI_API_KEY` is not set.
