# 09-gpt-5-3-codex / 04-ruby-factorial

E2E smoke test: `gpt-5.3-codex` generates a Ruby ruby-factorial program, a tool node extracts and runs it, and the test validates the expected output. Skips if `OPENAI_API_KEY` is not set.
