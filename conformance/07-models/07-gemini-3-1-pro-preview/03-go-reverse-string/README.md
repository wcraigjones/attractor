# 07-gemini-3-1-pro-preview / 03-go-reverse-string

E2E smoke test: `gemini-3.1-pro-preview` generates a Go go-reverse-string program, a tool node extracts and runs it, and the test validates the expected output. Skips if `GEMINI_API_KEY` is not set.
