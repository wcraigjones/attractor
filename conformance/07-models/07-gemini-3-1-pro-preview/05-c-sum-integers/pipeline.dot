digraph smoke_gemini_3_1_pro_05_c_sum_integers {
    graph [goal="Generate and run a C sum-of-integers program"]

    start [shape=Mdiamond]
    code  [shape=box, llm_model="gemini-3.1-pro-preview", prompt="Write a C program that sums the integers from 1 to 100 and prints the result. Output ONLY the code inside a single fenced code block. No explanations."]
    run   [shape=parallelogram, tool_command="bash \"$EXTRACT_RUN\" c code"]
    done  [shape=Msquare]

    start -> code
    code  -> run
    run   -> done
}
