# 04-gpt-5-2 / 06-bash-prime-checker

E2E smoke test: `gpt-5.2` generates a Bash bash-prime-checker program, a tool node extracts and runs it, and the test validates the expected output. Skips if `OPENAI_API_KEY` is not set.
