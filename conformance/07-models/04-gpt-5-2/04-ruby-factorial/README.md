# 04-gpt-5-2 / 04-ruby-factorial

E2E smoke test: `gpt-5.2` generates a Ruby ruby-factorial program, a tool node extracts and runs it, and the test validates the expected output. Skips if `OPENAI_API_KEY` is not set.
