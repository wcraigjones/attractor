# 06-gpt-5-2-codex / 01-python-fizzbuzz

E2E smoke test: `gpt-5.2-codex` generates a Python python-fizzbuzz program, a tool node extracts and runs it, and the test validates the expected output. Skips if `OPENAI_API_KEY` is not set.
