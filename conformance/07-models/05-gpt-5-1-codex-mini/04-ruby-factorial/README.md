# 05-gpt-5-1-codex-mini / 04-ruby-factorial

E2E smoke test: `gpt-5.1-codex-mini` generates a Ruby ruby-factorial program, a tool node extracts and runs it, and the test validates the expected output. Skips if `OPENAI_API_KEY` is not set.
