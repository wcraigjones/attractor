digraph smoke_gpt_5_1_codex_mini_08_python_multi_file {
    graph [goal="Generate a multi-file Python module and write files to disk"]

    start [shape=Mdiamond]
    code  [shape=box, llm_model="gpt-5.1-codex-mini", prompt="Create two Python files. File 1 named utils.py: define a function called greet that takes a name parameter and returns the string \"Hello, \" followed by the name followed by \"!\". File 2 named main.py: import greet from utils and print the result of calling greet with \"Attractor\" as the argument. Output each file in its own fenced code block. Put a line containing just the filename before each code block. No other explanations."]
    run   [shape=parallelogram, tool_command="bash \"$EXTRACT_WRITE_RUN\" python code"]
    done  [shape=Msquare]

    start -> code
    code  -> run
    run   -> done
}
