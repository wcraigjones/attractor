# 05-gpt-5-1-codex-mini / 05-c-sum-integers

E2E smoke test: `gpt-5.1-codex-mini` generates a C c-sum-integers program, a tool node extracts and runs it, and the test validates the expected output. Skips if `OPENAI_API_KEY` is not set.
