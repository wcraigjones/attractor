# 05-gpt-5-1-codex-mini / 07-python-temp-converter

E2E smoke test: `gpt-5.1-codex-mini` generates a Python python-temp-converter program, a tool node extracts and runs it, and the test validates the expected output. Skips if `OPENAI_API_KEY` is not set.
